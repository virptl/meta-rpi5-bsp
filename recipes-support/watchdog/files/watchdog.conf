# /etc/watchdog.conf

# Point to the kernel watchdog device
watchdog-device = /dev/watchdog

# Reset the board if not petted within 15 seconds
watchdog-timeout = 15

# Realtime priority ensures the daemon runs even under high load
realtime = yes
priority = 1
